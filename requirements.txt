# requirements.txt — CNN + LLM (compatible pins)
streamlit>=1.39
torch>=2.3.0,<3.0
torchvision>=0.18.0,<1.0
timm>=0.9.12,<1.0
pillow>=10.3.0
requests>=2.32.0

# LLM deps (match transformers that requires hub < 1.0)
transformers==4.44.2
huggingface-hub>=0.34,<1.0
accelerate>=0.33.0
safetensors>=0.4.2
sentencepiece>=0.2.0
